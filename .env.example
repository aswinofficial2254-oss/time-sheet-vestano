# Gmail example: use a Google App Password, not the normal mailbox password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=timesheet@vestano.example
SMTP_PASSWORD=replace-with-app-password
SMTP_FROM=timesheet@vestano.example
